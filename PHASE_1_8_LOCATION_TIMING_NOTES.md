# Phase 1.8 — Location + Timing Confluence

Backtest-only. Replaces `backtest/bt_active_v8.py`.

Adds OR30, VWAP distance/state, ATR extension, recent 30m/60m move, `edge_by_location.csv`, and non-fatal final summary writers so long GitHub runs still upload artifacts if a late summary/report writer fails.
